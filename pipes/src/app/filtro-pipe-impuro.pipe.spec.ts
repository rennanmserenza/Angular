import { FiltroPipeImpuroPipe } from './filtro-pipe-impuro.pipe';

describe('FiltroPipeImpuroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPipeImpuroPipe();
    expect(pipe).toBeTruthy();
  });
});
